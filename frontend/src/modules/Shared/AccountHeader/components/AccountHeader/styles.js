import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    position: "absolute",
    top: 5,
    right: 5,
    display: "flex",
    flexDirection: "row",
    alignItems: "center"
  },
  text: {
    padding: "0 10px"
  },
  icon: {
    fontSize: 24,
    color: "gray"
  }
});

export default styles;
